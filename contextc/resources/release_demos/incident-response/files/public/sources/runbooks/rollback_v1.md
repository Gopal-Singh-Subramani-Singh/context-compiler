# Checkout rollback runbook v1 (superseded)
Older procedure: restart all checkout workers first and flush the shared cache before considering a rollback. This procedure has been superseded and must not be used for the current incident.
